{
  "devDependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.8",
    "clean-css-cli": "^5.6.3",
    "cross-env": "^10.1.0",
    "esbuild": "^0.28.1",
    "html-minifier-terser": "^7.2.0",
    "katex": "^0.17.0",
    "purgecss": "^8.0.0",
    "shx": "^0.4.0"
  },
  "overrides": {
    "picomatch": "^2.3.2"
  },
  "scripts": {
    "clean": "shx rm -rf _site",
    "build:jekyll": "cross-env JEKYLL_ENV=production bundle exec jekyll build",
    "optimize:math": "node optimize-math.mjs",
    "optimize:css": "node optimize-css.mjs",
    "optimize:js": "node optimize-js.mjs",
    "optimize:hash": "node fingerprint.mjs",
    "optimize:html": "html-minifier-terser --collapse-whitespace --remove-comments --collapse-boolean-attributes --remove-redundant-attributes --remove-empty-attributes --remove-style-link-type-attributes --remove-script-type-attributes --decode-entities --use-short-doctype --sort-attributes --sort-class-name --minify-js true --minify-css true --input-dir _site --output-dir _site --file-ext html",
    "sitemap:lastmod": "node update-sitemap.mjs",
    "check": "node verify.mjs",
    "publish:docs": "node publish.mjs",
    "build": "npm run clean && npm run build:jekyll && npm run optimize:math && npm run optimize:css && npm run optimize:js && npm run optimize:hash && npm run optimize:html && npm run sitemap:lastmod && npm run check && npm run publish:docs",
    "ship": "node ship.mjs",
    "preview": "node preview.mjs"
  }
}
